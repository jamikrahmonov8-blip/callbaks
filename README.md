## Objekt - это функция пахожое на array катори принимает внутри себя все tuypof
### Это функция каторое принимает даные ползователя мы эти данные получае от бикеншиков и исползуем это в наших проектов 
- ### Create objekt - у нас есть одын способ чтобы эго напысать 
  ### -  Мы как вызаем array  вот так [ ] а object вот так{ }     
  пример 
  ```js
  let odam= {
    name: "jamshed", 
    age:16, 
    location: "Dushanbe",
    number : {
        bab:"90818282",
        meg:"10029122",
    },
  abuot : () => {return `Is ${odam.name}`} 
  }  
  ```
  ### У нас есть две вещи это ключ и эго вывод - мы сначала пишем ключ потом пишем : - а то без етого наш код работат небудут мы этим показивам что это ключ потом мы пишем его значения эго  уже надо писать с обозначением string ато код пошитает что это не верно
  # есть два способа для вызова это 
  - ### don notation 
  - ### square brackets
### вот его обекти сами ишите в интернете 
```js
console.log(Object.keys(odam));
console.log(Object.values(odam));
console.log(Object.entries(odam));
console.log(Object.fromEntries(arr));
``` 
   